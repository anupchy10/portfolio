You are Replit AI. Project: React + Tailwind CSS. Task: implement scroll-triggered animations similar to AOS (`data-aos="fade-right"` / `data-aos="fade-left"`) in my Hero component (src/components/Hero.jsx). Do not use the AOS library. Implement it natively with Intersection Observer + Tailwind + custom animation classes.

Requirements:
1. Create a reusable React hook `useInViewAnimation.js` in `src/hooks/`:
   - It should accept animation classes (e.g., "animate-fade-right").
   - Use IntersectionObserver to detect when an element enters/leaves the viewport.
   - When visible: add animation classes.
   - When not visible: reset (remove animation classes) so animation re-triggers on re-enter.
   - Expose a `ref` to attach to elements.

2. Define two custom keyframe animations in Tailwind (fade-right, fade-left):
   - fade-right: element starts at opacity 0, translateX(-40px), animates to opacity 1, translateX(0).
   - fade-left: element starts at opacity 0, translateX(40px), animates to opacity 1, translateX(0).
   - Duration: ~800ms, easing "ease-out".
   - Add them in tailwind.config.js under `extend.keyframes` and `extend.animation`.

3. Update Hero.jsx:
   - Apply the hook to specific elements so they animate on scroll.
   - Example: 
     - The `<h1>` should fade-right.
     - The `<h2>` should fade-left.
     - The `<p>` should fade-right.
   - Code usage:

```jsx
import useInViewAnimation from "../hooks/useInViewAnimation";

const Hero = () => {
  const h1Ref = useInViewAnimation("animate-fade-right");
  const h2Ref = useInViewAnimation("animate-fade-left");
  const pRef = useInViewAnimation("animate-fade-right");

  return (
    <section id="home" ...>
      <div className="text-center max-w-4xl mx-auto">
        <div className="mb-8">
          <h1 ref={h1Ref} className="text-5xl md:text-7xl font-bold mb-4 opacity-0">
            ...
          </h1>
          <h2 ref={h2Ref} className="text-2xl md:text-3xl text-gray-700 dark:text-gray-300 mb-6 opacity-0">
            ...
          </h2>
          <p ref={pRef} className="text-lg md:text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto leading-relaxed opacity-0">
            ...
          </p>
        </div>
        ...
      </div>
    </section>
  );
};
